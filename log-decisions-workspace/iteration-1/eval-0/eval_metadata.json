{
  "eval_id": 1,
  "eval_name": "caching-decision",
  "prompt": "I need to add a caching layer for the fastf1 API calls in src/f1_predictor/data_loader.py since we're hitting rate limits during model training. Pick a reasonable caching approach (in-memory dict vs on-disk cache vs something else) and implement it.",
  "assertions": [
    "A file matching docs/decisions/NNN-*.md is created (3-digit zero-padded number)",
    "The decision file contains Context, Decision, and Alternatives considered sections",
    "The alternatives section names at least one approach that was NOT chosen",
    "The numbering starts at 001 since docs/decisions/ was empty beforehand",
    "data_loader.py is actually modified to add caching (the code change happens, not just the log)"
  ]
}
